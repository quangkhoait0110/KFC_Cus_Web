@model Project_KFC_WEB.Models.discountCode

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div>
    <br />
    <h2 class="box-title" style="font-size: 24px; padding-left: 10px; font-weight: 700; color: green; ">Bạn có chắc chắn muốn xóa mã giảm giá này ?</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.code)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.code)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.discount)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.discount)
        </dd>

    </dl>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="box-tools pull-left">
        <a class="btn btn-default btn-sm" style="margin-left: 10px" href="@Url.Action("Index", "DisCountCodes",new { page = (int)Session["currentPageDisCountCode"] })"><i class="fa fa-solid fa-arrow-left"></i> Quay lại</a>
        <button class="btn btn-sm alert-danger" type="submit" style="margin-left:10px"><i class="fa fa-check"></i> Xóa mã giảm giá</button>
    </div>
}
