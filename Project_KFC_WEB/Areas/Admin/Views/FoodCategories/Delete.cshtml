@model Project_KFC_WEB.Models.foodCategory

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<div>
    <br />
    <h2 class="box-title" style="font-size: 24px; padding-left: 10px; font-weight: 700; color: green; ">Bạn có chắc chắn muốn xóa loại thức ăn này ?</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.image)
        </dt>

        <dd>
            <img src="~/Upload/@Model.image" style="width: 200px; height:100%" />
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="box-tools pull-left">
            <a class="btn btn-default btn-sm" style="margin-left: 10px" href="@Url.Action("Index", "FoodCategories",new { page = (int)Session["currentPageFoodCategory"] })"><i class="fa fa-solid fa-arrow-left"></i> Quay lại</a>
            <button class="btn btn-sm alert-danger" type="submit" style="margin-left:10px"><i class="fa fa-check"></i> Xóa loại thức ăn</button>
        </div>
    }
</div>
